npx create-node-server@latest ai-friends-app && cd ai-friends-app && npm install express body-parser cors bcrypt jsonwebtoken openai && echo "const express = require('express'); const bodyParser = require('body-parser'); const cors = require('cors'); const bcrypt = require('bcrypt'); const jwt = require('jsonwebtoken'); const { Configuration, OpenAIApi } = require('openai'); const app = express(); app.use(cors()); app.use(bodyParser.json()); const SECRET = 'your_jwt_secret'; const configuration = new Configuration({ apiKey: 'YOUR_OPENAI_API_KEY' }); const openai = new OpenAIApi(configuration); const users = []; app.post('/signup', async (req, res) => { const { username, password } = req.body; if (users.find(u => u.username === username)) return res.status(400).json({ error: 'User exists' }); const hash = await bcrypt.hash(password, 10); users.push({ username, password: hash, friends: [], requests: [] }); res.json({ message: 'Signup success' }); }); app.post('/login', async (req, res) => { const { username, password } = req.body; const user = users.find(u => u.username === username); if (!user || !(await bcrypt.compare(password, user.password))) return res.status(401).json({ error: 'Invalid credentials' }); const token = jwt.sign({ username }, SECRET); res.json({ token }); }); const auth = (req, res, next) => { const token = req.headers.authorization?.split(' ')[1]; try { req.user = jwt.verify(token, SECRET); next(); } catch { res.status(401).json({ error: 'Unauthorized' }); } }; app.post('/add-friend', auth, (req, res) => { const { to } = req.body; const user = users.find(u => u.username === req.user.username); const friend = users.find(u => u.username === to); if (!friend || friend.requests.includes(user.username)) return res.status(400).json({ error: 'Invalid' }); friend.requests.push(user.username); res.json({ message: 'Request sent' }); }); app.post('/accept-friend', auth, (req, res) => { const { from } = req.body; const user = users.find(u => u.username === req.user.username); const requester = users.find(u => u.username === from); if (!requester || !user.requests.includes(from)) return res.status(400).json({ error: 'No request' }); user.friends.push(from); requester.friends.push(user.username); user.requests = user.requests.filter(r => r !== from); res.json({ message: 'Friend added' }); }); app.get('/friends', auth, (req, res) => { const user = users.find(u => u.username === req.user.username); res.json({ friends: user.friends }); }); app.post('/chat', auth, async (req, res) => { const { message, subject, group } = req.body; try { const completion = await openai.createChatCompletion({ model: 'gpt-3.5-turbo', messages: [ { role: 'system', content: `You're a tutor for ${subject || 'General'} in group ${group || 'Default'}` }, { role: 'user', content: message } ] }); res.json({ reply: completion.data.choices[0].message.content }); } catch (err) { res.status(500).json({ error: err.message }); } }); app.listen(3000, () => console.log('Server running')); " > index.js && echo "<!DOCTYPE html><html><body><h2>Login / Signup</h2><input id='user' placeholder='Username'><input id='pass' placeholder='Password' type='password'><button onclick='signup()'>Signup</button><button onclick='login()'>Login</button><br><hr><h2>Friend</h2><input id='to' placeholder='Friend username'><button onclick='addFriend()'>Add</button><input id='from' placeholder='Friend request from'><button onclick='acceptFriend()'>Accept</button><hr><h2>Chat</h2><input id='subject' placeholder='Subject'><input id='group' placeholder='Group'><br><input id='msg' placeholder='Your message'><button onclick='chat()'>Send</button><textarea id='chatbox' rows='10' cols='50'></textarea><script>let token='';async function signup(){const r=await fetch('/signup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:user.value,password:pass.value})});alert((await r.json()).message)}async function login(){const r=await fetch('/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:user.value,password:pass.value})});const d=await r.json();token=d.token;alert('Login success')}async function addFriend(){await fetch('/add-friend',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify({to:to.value})})}async function acceptFriend(){await fetch('/accept-friend',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify({from:from.value})})}async function chat(){const r=await fetch('/chat',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify({message:msg.value,subject:subject.value,group:group.value})});const d=await r.json();chatbox.value+=`\\nYou: ${msg.value}\\nAI: ${d.reply}`}</script></body></html>" > index.html && echo '{ "name": "ai-friends-app", "version": "1.0.0", "main": "index.js", "scripts": { "start": "node index.js" }, "dependencies": { "bcrypt": "^5.1.0", "body-parser": "^1.20.0", "cors": "^2.8.5", "express": "^4.18.2", "jsonwebtoken": "^9.0.0", "openai": "^4.0.0" } }' > package.json
